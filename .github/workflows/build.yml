name: Universal Cross-Platform App Builder (Pro)

on:
  workflow_dispatch:
    inputs:
      target_platform:
        description: 'בחר פלטפורמה ליצירה'
        required: true
        default: 'windows'
        type: choice
        options:
          - windows
          - android
      website_url:
        description: 'כתובת אתר מלאה (למשל https://example.com) - השאר ריק אם משתמש בקובץ מקומי'
        required: false
        default: ''
        type: string
      local_html_file:
        description: 'קובץ HTML מקומי ראשי (רלוונטי רק אם כתובת האתר ריקה)'
        required: false
        default: 'index.html'
        type: string
      app_name:
        description: 'שם התוכנה / אפליקציה'
        required: true
        default: 'MyCoolApp'
        type: string
      app_version:
        description: 'גרסת התוכנה (למשל 1.0.0)'
        required: true
        default: '1.0.0'
        type: string

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js (with caching)
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Pre-flight & Icon Fetching
        shell: pwsh
        run: |
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
          $url = "${{ github.event.inputs.website_url }}".Trim()
          $local = "${{ github.event.inputs.local_html_file }}".Trim()
          
          if (-not [string]::IsNullOrWhiteSpace($url)) {
            Write-Host ">>> מקור נבחר: כתובת אינטרנט -> $url"
            echo "FINAL_SOURCE=$url" >> $env:GITHUB_ENV
            echo "IS_URL=true" >> $env:GITHUB_ENV
          } else {
            Write-Host ">>> מקור נבחר: קובץ מקומי -> $local"
            if (-not (Test-Path $local)) {
              Write-Error "שגיאה: הקובץ המקומי '$local' לא נמצא בפרויקט!"
              exit 1
            }
            echo "FINAL_SOURCE=$local" >> $env:GITHUB_ENV
            echo "IS_URL=false" >> $env:GITHUB_ENV
          }

          # מערכת חילוץ אייקונים חכמה
          $iconFound = $false
          if (Test-Path "icon.png") { 
              $iconFound = $true 
          } elseif (-not [string]::IsNullOrWhiteSpace($url)) {
              try {
                  $domain = ([System.Uri]$url).Host
                  $faviconApi = "https://www.google.com/s2/favicons?domain=$domain&sz=128"
                  Write-Host ">>> מוריד אייקון עבור הדומיין: $domain"
                  Invoke-WebRequest -Uri $faviconApi -OutFile "icon.png" -UserAgent "Mozilla/5.0"
                  if (Test-Path "icon.png") { $iconFound = $true }
              } catch {
                  Write-Host ">>> לא ניתן היה להוריד אייקון חיצוני."
              }
          }
          if ($iconFound) { echo "ICON_AVAILABLE=true" >> $env:GITHUB_ENV }

      # ==========================================
      # מסלול א': בניית תוכנת Windows (EXE) + חתימה
      # ==========================================
      - name: Setup .NET SDK
        if: ${{ github.event.inputs.target_platform == 'windows' }}
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Compile Windows EXE
        if: ${{ github.event.inputs.target_platform == 'windows' }}
        shell: pwsh
        run: |
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
          Write-Host ">>> יוצר פרויקט C# WinForms..."
          dotnet new winforms -n UniversalAppProject -o . --force
          dotnet add package Microsoft.Web.WebView2

          $appVersion = "${{ github.event.inputs.app_version }}"
          $appName = "${{ github.event.inputs.app_name }}"
          $csproj = Get-Content UniversalAppProject.csproj -Raw
          $csproj = $csproj -replace '<OutputType>Exe</OutputType>', '<OutputType>WinExe</OutputType>'
          
          $versionProps = @"
            <Version>$appVersion</Version>
            <AssemblyVersion>$appVersion</AssemblyVersion>
            <FileVersion>$appVersion</FileVersion>
            <Product>$appName</Product>
            <Company>ינון דרייב</Company>
          "@
          $csproj = $csproj -replace '</PropertyGroup>', "$versionProps`n  </PropertyGroup>"
          
          if ($env:ICON_AVAILABLE -eq 'true') {
              Write-Host ">>> ממיר תמונה לאייקון .ico..."
              cmd.exe /c "npx --yes png-to-ico icon.png > icon.ico"
              if (Test-Path "icon.ico") {
                  $csproj = $csproj -replace '</PropertyGroup>', "<ApplicationIcon>icon.ico</ApplicationIcon>`n  </PropertyGroup>"
              }
          }
          
          Set-Content UniversalAppProject.csproj -Value $csproj -Encoding UTF8

          $safeAppName = $appName -replace '"', '\"'
          $safeSource = $env:FINAL_SOURCE -replace '"', '\"' -replace '\\', '\\\\'
          $isUrl = $env:IS_URL

          $formCode = @"
          using System;
          using System.IO;
          using System.Windows.Forms;
          using System.Diagnostics;
          using Microsoft.Web.WebView2.Core;

          namespace UniversalAppProject
          {
              static class Program
              {
                  [STAThread]
                  static void Main()
                  {
                      Application.EnableVisualStyles();
                      Application.SetCompatibleTextRenderingDefault(false);
                      Application.Run(new MainForm());
                  }
              }

              public partial class MainForm : Form
              {
                  private Microsoft.Web.WebView2.WinForms.WebView2? webView;

                  public MainForm()
                  {
                      this.Text = `"$safeAppName`";
                      this.Width = 1024;
                      this.Height = 768;
                      this.StartPosition = FormStartPosition.CenterScreen;
                      
                      try
                      {
                          string? exePath = Process.GetCurrentProcess().MainModule?.FileName;
                          if (!string.IsNullOrEmpty(exePath))
                          {
                              this.Icon = System.Drawing.Icon.ExtractAssociatedIcon(exePath);
                          }
                      }
                      catch { }

                      LoadWebView();
                  }

                  private async void LoadWebView()
                  {
                      webView = new Microsoft.Web.WebView2.WinForms.WebView2();
                      webView.Dock = DockStyle.Fill;
                      this.Controls.Add(webView);
                      
                      try
                      {
                          await webView.EnsureCoreWebView2Async(null);
                          string target = `"$safeSource`";
                          bool isUrl = $isUrl;

                          if (isUrl) 
                          {
                              webView.CoreWebView2.Navigate(target);
                          } 
                          else 
                          {
                              string exeDir = AppDomain.CurrentDomain.BaseDirectory;
                              webView.CoreWebView2.SetVirtualHostNameToFolderMapping("app.local", exeDir, CoreWebView2HostResourceAccessKind.Allow);
                              string urlPath = target.Replace("\\", "/");
                              webView.CoreWebView2.Navigate($"https://app.local/{urlPath}");
                          }
                      }
                      catch (Exception ex)
                      {
                          MessageBox.Show("שגיאה בטעינת האפליקציה: " + ex.Message, "שגיאה", MessageBoxButtons.OK, MessageBoxIcon.Error);
                      }
                  }
              }
          }
          "@
          Set-Content -Path "Program.cs" -Value $formCode -Encoding UTF8

          dotnet publish -c Release -r win-x64 -p:OutputType=WinExe -o ./final_publish --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:EnableCompressionInSingleFile=true
          New-Item -ItemType Directory -Force -Path "build_output"
          Get-ChildItem -Path "./final_publish" -Filter "*.exe" | Copy-Item -Destination "build_output/"

      - name: Sign Windows Executable
        if: ${{ github.event.inputs.target_platform == 'windows' && env.CERTIFICATE_BASE64 != '' }}
        shell: pwsh
        env:
          CERT_BASE64: ${{ secrets.CERTIFICATE_BASE64 }}
          CERT_PASSWORD: ${{ secrets.CERTIFICATE_PASSWORD }}
        run: |
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
          if ([string]::IsNullOrWhiteSpace($env:CERT_BASE64)) {
              Write-Host ">>> לא נמצאה תעודת חתימה, מדלג..."
              exit 0
          }
          Write-Host ">>> חותם על ה-EXE..."
          $certBytes = [System.Convert]::FromBase64String($env:CERT_BASE64)
          $certPath = "$env:TEMP\temp_cert.pfx"
          [System.IO.File]::WriteAllBytes($certPath, $certBytes)
          $signtool = (Get-ChildItem "C:\Program Files*\Windows Kits\10\bin\*\x64\signtool.exe" | Select-Object -First 1).FullName
          $exeToSign = Get-ChildItem -Path "build_output" -Filter "*.exe" -File | Select-Object -First 1
          & $signtool sign /v /f $certPath /p "$env:CERT_PASSWORD" /tr http://timestamp.digicert.com /td sha256 /fd SHA256 $exeToSign.FullName

      - name: Cleanup Windows Certificate
        if: ${{ always() && github.event.inputs.target_platform == 'windows' }}
        shell: pwsh
        run: Remove-Item "$env:TEMP\temp_cert.pfx" -Force -ErrorAction SilentlyContinue

      # ==========================================
      # מסלול ב': בניית אפליקציית Android (APK)
      # ==========================================
      - name: Setup Java SDK (with Gradle cache)
        if: ${{ github.event.inputs.target_platform == 'android' }}
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Setup Android SDK
        if: ${{ github.event.inputs.target_platform == 'android' }}
        uses: android-actions/setup-android@v3

      - name: Compile Android APK
        if: ${{ github.event.inputs.target_platform == 'android' }}
        shell: pwsh
        run: |
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
          Write-Host ">>> מתקין Cordova..."
          npm install -g cordova

          Write-Host ">>> יוצר פרויקט אנדרואיד..."
          cordova create android_app com.yinon.app "${{ github.event.inputs.app_name }}"
          
          if ($env:ICON_AVAILABLE -eq 'true') {
              Copy-Item "icon.png" "android_app/icon.png" -Force
          }

          Set-Location android_app
          cordova platform add android

          $source = $env:FINAL_SOURCE
          $isUrl = $env:IS_URL
          $configPath = "config.xml"
          $configContent = Get-Content $configPath -Raw

          if ($env:ICON_AVAILABLE -eq 'true') {
              $configContent = $configContent -replace '</widget>', "<icon src=`"icon.png`" />`n</widget>"
          }

          if ($isUrl -eq 'true') {
            Write-Host ">>> הגדרת URL באפליקציה: $source"
            $configContent = $configContent -replace '<content src="index.html" />', "<content src=`"$source`" />"
            $configContent = $configContent -replace '</widget>', "<access origin=`"*`" />`n<allow-navigation href=`"*`" />`n</widget>"
          } else {
            Write-Host ">>> מעתיק קבצים מקומיים ל-Assets..."
            Get-ChildItem -Path .. -Exclude "android_app", "icon.png", ".git", ".github" | Copy-Item -Destination "www" -Recurse -Force
          }
          
          Set-Content $configPath -Value $configContent -Encoding UTF8

          Write-Host ">>> מקמפל APK..."
          cordova build android --release

          New-Item -ItemType Directory -Force -Path "../build_output"
          Get-ChildItem -Path "platforms/android/app/build/outputs/apk/release" -Filter "*.apk" | Copy-Item -Destination "../build_output/"

      # ==========================================
      # סיום והעלאת קבצים
      # ==========================================
      - name: Artifact Upload
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.inputs.app_name }}-${{ github.event.inputs.target_platform }}-Build
          path: build_output/
          retention-days: 7
